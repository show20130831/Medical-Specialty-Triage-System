services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      TRIAGE_MODEL_DIR: /models/pubmedbert_description
      TRIAGE_HOST: 0.0.0.0
      TRIAGE_PORT: 8000
    volumes:
      - ./models:/models:ro
